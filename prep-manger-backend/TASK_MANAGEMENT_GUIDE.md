# دليل نظام إدارة المهام المتكامل

## 📋 نظرة عامة

تم تطوير نظام إدارة مهام متكامل لتنظيم وتتبع مهام فريق العمل بشكل فعال. النظام يوفر:

- ✅ إدارة المهام حسب النوع والأولوية
- ✅ نظام تنبيهات ذكي مع التصعيد التلقائي
- ✅ لوحة تحكم KPIs شاملة
- ✅ تقارير يومية وأسبوعية وشهرية
- ✅ نظام تعليقات وسجل تنفيذ للمهام
- ✅ لوحة Kanban لإدارة المهام

---

## 🎯 أنواع المهام (Task Types)

### 1. مهمة تنبيه أحمر (Red Alert Task)
- **المدة**: ≤ 10 دقائق
- **الأولوية**: حرجة أو عالية
- **الاستخدام**: مهام عاجلة جداً تحتاج تنفيذ فوري
- **مثال**: تحضير طلب VIP، طلب طارئ

```typescript
taskType: TaskType.RED_ALERT
estimatedDuration: 10 minutes (max)
priority: TaskPriority.CRITICAL
```

### 2. مهمة متوسطة (Medium Task)
- **المدة**: ≤ 4 ساعات
- **الأولوية**: متوسطة أو عالية
- **الاستخدام**: مهام عادية تحتاج وقت معقول
- **مثال**: تحضير منتج للوجبة القادمة

```typescript
taskType: TaskType.MEDIUM
estimatedDuration: up to 240 minutes
priority: TaskPriority.MEDIUM
```

### 3. مهمة متكررة يومياً (Daily Recurring)
- **التكرار**: كل يوم
- **الاستخدام**: مهام يومية ثابتة
- **مثال**: تحضير الخبز الصباحي، تنظيف المطبخ

```typescript
taskType: TaskType.DAILY_RECURRING
isRecurring: true
recurringPattern: 'daily'
```

### 4. مهمة أسبوعية (Weekly Recurring)
- **التكرار**: أسبوعياً
- **الاستخدام**: مهام أسبوعية منتظمة
- **مثال**: جرد المخزون الأسبوعي

```typescript
taskType: TaskType.WEEKLY_RECURRING
isRecurring: true
recurringPattern: 'weekly'
```

### 5. مهمة فورية عند الطلب (On Demand)
- **التوقيت**: فوري
- **الاستخدام**: مهام غير مخطط لها
- **مثال**: طلب مفاجئ من العميل

```typescript
taskType: TaskType.ON_DEMAND
priority: varies based on urgency
```

---

## 🚨 نظام الأولويات (Priority System)

### Critical (حرجة)
- تحتاج إلى تنفيذ فوري
- تصعيد تلقائي عند التأخر لأكثر من ساعتين
- إشعارات متكررة كل 15 دقيقة

### High (عالية)
- مهمة عاجلة
- تصعيد عند التأخر لأكثر من 4 ساعات
- إشعارات كل 30 دقيقة

### Medium (متوسطة)
- مهمة عادية
- تنبيه عند اقتراب الموعد
- إشعار واحد عند التأخر

### Low (منخفضة)
- مهمة غير عاجلة
- إشعار بسيط عند التأخر

---

## 📊 الشاشات الرئيسية

### 1. شاشة اليوم (Today Tasks)
عرض جميع المهام المجدولة لليوم الحالي

**API Endpoint:**
```
GET /api/tasks/today
```

**الحقول المعروضة:**
- اسم المهمة
- الوقت المجدول
- الحالة
- الأولوية
- الموظف المعين
- الوقت المتبقي

### 2. شاشة المهام الأسبوعية (Weekly Tasks)
عرض المهام للأسبوع الحالي

**API Endpoint:**
```
GET /api/tasks/weekly
```

### 3. شاشة المهام الفورية والعاجلة (Urgent Tasks)
عرض المهام الحرجة والعاجلة فقط

**API Endpoint:**
```
GET /api/tasks/urgent
```

**المعايير:**
- taskType = RED_ALERT
- taskType = ON_DEMAND
- priority = CRITICAL
- isOverdue = true

### 4. لوحة Kanban

**API Endpoint:**
```
GET /api/tasks/kanban
```

**الأعمدة:**
- **Pending**: مهام مجدولة أو معلقة
- **In Progress**: مهام قيد التنفيذ
- **Done**: مهام مكتملة

### 5. صفحة تفاصيل المهمة

**API Endpoint:**
```
GET /api/tasks/:taskId
```

**المعلومات المتاحة:**
- تفاصيل المهمة الكاملة
- التعليقات
- سجل التنفيذ
- الإشعارات المرتبطة

---

## 💬 نظام التعليقات

### إضافة تعليق
```typescript
POST /api/tasks/:taskId/comments
{
  comment: string,
  attachments?: string[]
}
```

### جلب التعليقات
```typescript
GET /api/tasks/:taskId/comments
```

### تحديث تعليق
```typescript
PUT /api/tasks/comments/:commentId
{
  comment: string
}
```

### حذف تعليق
```typescript
DELETE /api/tasks/comments/:commentId
```

---

## 📝 سجل التنفيذ (Execution Log)

يتم تسجيل جميع الأحداث التالية تلقائياً:

- إنشاء المهمة
- تعيين المهمة
- بدء المهمة
- إيقاف مؤقت / استئناف
- إكمال المهمة
- التصعيد
- تغيير الحالة
- إضافة تعليق

**API Endpoint:**
```
GET /api/tasks/:taskId/execution-log
```

---

## 🔔 نظام التنبيهات والإشعارات

### أنواع الإشعارات

#### 1. BEFORE_DUE (قبل الموعد)
- يتم إرساله قبل 30 دقيقة من موعد المهمة
- مستوى: INFO
- يتطلب إجراء: نعم

#### 2. OVERDUE (متأخر)
- يتم إرساله عند تجاوز الموعد المحدد
- مستوى: WARNING
- يتطلب إجراء: نعم

#### 3. CRITICAL (حرج)
- للمهام الحرجة غير المنجزة
- مستوى: CRITICAL
- يتطلب إجراء: نعم

#### 4. ESCALATION (تصعيد)
- عند تصعيد المهمة للمدير
- مستوى: CRITICAL
- يتطلب إجراء: نعم

### التصعيد التلقائي

**المعايير:**
- المهمة متأخرة لأكثر من ساعتين
- الأولوية: CRITICAL أو HIGH
- لم يتم تصعيدها مسبقاً
- الحالة: ليست مكتملة

**الوظيفة:**
```typescript
await autoEscalateOverdueTasks();
```

**يتم تنفيذها كل:** 15 دقيقة (عبر cron job)

---

## 📈 لوحة التحكم (Dashboard)

### مؤشرات الأداء الرئيسية (KPIs)

**API Endpoint:**
```
GET /api/dashboard
Query Parameters: ?dateFrom=YYYY-MM-DD&dateTo=YYYY-MM-DD
```

**البيانات المعروضة:**

#### 1. إحصائيات المهام
- إجمالي المهام
- المهام المكتملة
- المهام قيد التنفيذ
- المهام المتأخرة
- معدل الإنجاز
- معدل الإنجاز في الوقت المحدد
- متوسط وقت الإنجاز

#### 2. أداء الفريق
- عدد أعضاء الفريق
- الأعضاء النشطون
- المهام لكل عضو
- معدل الإنجاز لكل عضو
- أفضل 5 موظفين

#### 3. توزيع المهام
- حسب النوع
- حسب الأولوية
- حسب الحالة

#### 4. تحليل الوقت
- المهام حسب الساعة
- المهام حسب اليوم
- أوقات الذروة
- مقارنة التقدير بالفعلي

#### 5. التنبيهات الحرجة
- المهام المتأخرة
- المهام المصعدة
- الإشعارات الحرجة

#### 6. الاتجاهات
- اتجاه معدل الإنجاز
- اتجاه نسبة التأخير

---

## 📊 نظام التقارير

### 1. التقرير اليومي (Daily Report)

**API Endpoint:**
```
GET /api/reports/daily
Query: ?date=YYYY-MM-DD
```

**البيانات:**
- إجمالي المهام لليوم
- المهام المكتملة
- معدل الإنجاز
- التوزيع حسب النوع والأولوية
- أفضل الموظفين
- عدد التأخيرات
- الإنجازات المبكرة

### 2. التقرير الأسبوعي (Weekly Report)

**API Endpoint:**
```
GET /api/reports/weekly
Query: ?weekStart=YYYY-MM-DD
```

**البيانات:**
- إحصائيات الأسبوع
- التفصيل اليومي
- أداء الفريق
- الاتجاهات الأسبوعية

### 3. التقرير الشهري (Monthly Report)

**API Endpoint:**
```
GET /api/reports/monthly
Query: ?month=1-12&year=YYYY
```

**البيانات:**
- إحصائيات الشهر
- التفصيل الأسبوعي
- أداء الفريق مع ساعات العمل
- رؤى وتحليلات:
  - أفضل يوم
  - أسوأ يوم
  - أوقات الذروة
  - مجالات التحسين

---

## 🎯 نظام إدارة الوقت

### حساب موعد الانتهاء (Due Date)

```typescript
function calculateDueDate(scheduledAt, taskType, prepTimeMinutes) {
  switch (taskType) {
    case RED_ALERT:
      return scheduledAt + 10 minutes
    case MEDIUM:
      return scheduledAt + 4 hours
    case ON_DEMAND:
      return scheduledAt + prepTimeMinutes
    case DAILY_RECURRING:
    case WEEKLY_RECURRING:
      return scheduledAt + prepTimeMinutes + 30 minutes
    default:
      return scheduledAt + prepTimeMinutes
  }
}
```

### حساب المدة التقديرية

```typescript
function calculateEstimatedDuration(taskType, prepTimeMinutes) {
  switch (taskType) {
    case RED_ALERT:
      return Math.min(10, prepTimeMinutes)
    case MEDIUM:
      return Math.min(240, prepTimeMinutes) // 4 hours max
    default:
      return prepTimeMinutes
  }
}
```

### تقييم الأداء

```typescript
performanceScore = {
  ratio = actualDuration / estimatedDuration
  
  if (ratio <= 1.0) return 100  // ممتاز
  if (ratio <= 1.2) return 80   // جيد جداً
  if (ratio <= 1.5) return 60   // جيد
  return 40                      // يحتاج تحسين
}
```

---

## 👥 نظام التعيين

### تعيين لموظف محدد
```typescript
assignmentType: AssignmentType.SPECIFIC_USER
assignedTo: userId
assignedToName: userName
```

### تعيين لأي عضو من الفريق
```typescript
assignmentType: AssignmentType.ANY_TEAM_MEMBER
assignedTo: null // يمكن لأي شخص أخذ المهمة
```

---

## 🔄 دورة حياة المهمة (Task Lifecycle)

```
SCHEDULED → IN_PROGRESS → COMPLETED
    ↓
  LATE (إذا لم تبدأ في الموعد)
    ↓
  OVERDUE (إذا تجاوزت الموعد النهائي)
    ↓
  ESCALATED (تصعيد تلقائي)
```

### الحالات المتاحة:
- **SCHEDULED**: مجدولة
- **PENDING**: معلقة
- **IN_PROGRESS**: قيد التنفيذ
- **COMPLETED**: مكتملة
- **DONE**: منتهية
- **LATE**: متأخرة
- **OVERDUE**: متجاوزة الموعد
- **STOCK_ISSUE**: مشكلة في المخزون

---

## 🔧 الوظائف المساعدة (Cron Jobs)

### 1. تحديث المهام المتأخرة
```typescript
// كل 5 دقائق
schedule: '*/5 * * * *'
function: updateOverdueTasks()
```

### 2. إرسال تنبيهات المهام القريبة
```typescript
// كل 15 دقيقة
schedule: '*/15 * * * *'
function: sendUpcomingTaskReminders()
```

### 3. التصعيد التلقائي
```typescript
// كل 15 دقيقة
schedule: '*/15 * * * *'
function: autoEscalateOverdueTasks()
```

### 4. تنظيف البيانات القديمة
```typescript
// يومياً في 2:00 صباحاً
schedule: '0 2 * * *'
functions:
  - cleanOldNotifications(30 days)
  - cleanOldLogs(90 days)
```

---

## 📱 API Endpoints Summary

### المهام (Tasks)
```
GET    /api/tasks                  # جميع المهام
GET    /api/tasks/today            # مهام اليوم
GET    /api/tasks/weekly           # مهام الأسبوع
GET    /api/tasks/urgent           # المهام العاجلة
GET    /api/tasks/kanban           # لوحة Kanban
GET    /api/tasks/overdue          # المهام المتأخرة
GET    /api/tasks/escalated        # المهام المصعدة
GET    /api/tasks/:id              # تفاصيل مهمة
POST   /api/tasks                  # إنشاء مهمة
PUT    /api/tasks/:id              # تحديث مهمة
DELETE /api/tasks/:id              # حذف مهمة
POST   /api/tasks/:id/start        # بدء مهمة
POST   /api/tasks/:id/complete     # إكمال مهمة
POST   /api/tasks/:id/rate         # تقييم مهمة
```

### التعليقات (Comments)
```
GET    /api/tasks/:id/comments     # جلب التعليقات
POST   /api/tasks/:id/comments     # إضافة تعليق
PUT    /api/comments/:id           # تحديث تعليق
DELETE /api/comments/:id           # حذف تعليق
```

### سجل التنفيذ (Execution Log)
```
GET    /api/tasks/:id/execution-log    # سجل مهمة
GET    /api/users/:id/execution-log    # سجل موظف
GET    /api/execution-log/recent       # آخر الأنشطة
```

### الإشعارات (Notifications)
```
GET    /api/notifications              # جميع الإشعارات
GET    /api/notifications/unread       # غير المقروءة
GET    /api/notifications/critical     # الحرجة
PUT    /api/notifications/:id/read     # تعليم كمقروء
PUT    /api/notifications/read-all     # تعليم الكل كمقروء
```

### لوحة التحكم (Dashboard)
```
GET    /api/dashboard                  # بيانات Dashboard
GET    /api/dashboard/kpis             # مؤشرات الأداء
GET    /api/dashboard/team-performance # أداء الفريق
GET    /api/dashboard/alerts           # التنبيهات الحرجة
```

### التقارير (Reports)
```
GET    /api/reports/daily              # تقرير يومي
GET    /api/reports/weekly             # تقرير أسبوعي
GET    /api/reports/monthly            # تقرير شهري
```

---

## 🎨 أمثلة الاستخدام

### إنشاء مهمة عاجلة
```typescript
POST /api/tasks
{
  "productId": "64abc123...",
  "scheduledAt": "2024-01-15T10:00:00Z",
  "taskType": "red_alert",
  "priority": "critical",
  "assignedTo": "64user123...",
  "assignedToName": "أحمد محمد",
  "notes": "طلب VIP عاجل",
  "tags": ["vip", "urgent"]
}
```

### تصعيد مهمة يدوياً
```typescript
POST /api/tasks/:taskId/escalate
{
  "reason": "المهمة متأخرة ولم يتم التعامل معها"
}
```

### جلب بيانات Dashboard
```typescript
GET /api/dashboard?dateFrom=2024-01-01&dateTo=2024-01-31

Response:
{
  "kpis": {
    "totalTasks": 150,
    "completedTasks": 135,
    "completionRate": 90,
    "onTimeCompletionRate": 85,
    ...
  },
  "teamPerformance": {...},
  "taskDistribution": {...},
  "criticalAlerts": [...]
}
```

---

## 🔐 الصلاحيات والأدوار

### PREP (موظف تحضير)
- عرض المهام المعينة له
- بدء وإكمال المهام
- إضافة تعليقات
- عرض سجل التنفيذ

### SUPERVISOR (مشرف)
- جميع صلاحيات PREP
- تعيين المهام
- إنشاء مهام جديدة
- عرض أداء الفريق
- الوصول للتقارير

### MANAGER (مدير)
- جميع الصلاحيات
- الوصول الكامل للوحة التحكم
- استقبال المهام المصعدة
- إدارة الموظفين
- تعديل وحذف المهام

---

## 📌 ملاحظات مهمة

1. **التنبيهات الذكية**: يتم إرسال إشعارات تلقائية قبل موعد المهمة وعند التأخير
2. **التصعيد التلقائي**: المهام الحرجة المتأخرة يتم تصعيدها للمدير تلقائياً
3. **سجل التنفيذ**: كل إجراء يتم تسجيله للمراجعة والتدقيق
4. **التقييم**: يمكن تقييم أداء تنفيذ المهمة من 1-5
5. **الحقول الافتراضية**: يتم حساب الوقت المتبقي ونسبة الأداء تلقائياً

---

## 🚀 التحسينات المستقبلية المقترحة

1. دعم الإشعارات الفورية (Push Notifications)
2. تكامل مع تقويم Google/Outlook
3. تطبيق موبايل للموظفين
4. تحليلات متقدمة بالذكاء الاصطناعي
5. نظام مكافآت وتحفيز الموظفين

---

تم التطوير بواسطة فريق التطوير 🎯
